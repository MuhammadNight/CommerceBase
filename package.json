{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start":"json-server --watch Commerce.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}